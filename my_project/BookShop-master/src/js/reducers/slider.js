import initialState from '../constants/initialState';
import * as types from '../constants/actionTypes';


export default function slider(state = initialState.watchedBooks, action) {
    let { type, payload } = action;
            return state;
    
};